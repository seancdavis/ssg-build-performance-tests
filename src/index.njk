---
layout: layout
---

<div class="container py-8">
  <div class="text-center mb-12">
    <h1 class="text-3xl">Static Site Generators</h1>
    <h2 class="text-2xl">Build Performance Testing</h2>
  </div>

  {#
  TODO:

    1. Have run command accept a name for the dataset and use that as the count.
    2. Store results by dataset.
    3. Show a chart for each dataset.
        a. Bar chart for base.
        b. Line for small.
        c. Line for large.
    4. Add filter to show all, framework-only, or no-framework results. Update
       all charts in real time when choosing the filter.

  #}

  {#
    --- Hide the table ---
    We want to show relative results. The numbers are less important for these
    results.
  #}

  {# <div class="mb-12">
    <h3 class="text-lg font-bold mb-2 text-center">Average Results</h2>
    <table class="table-auto mx-auto">
      <thead>
        <th class="border px-4 py-2 bg-gray-200">Name</th>
        {% for label in results.labels %}
          <th class="border px-4 py-2 bg-gray-200">{{ label }}</th>
        {% endfor %}
      </thead>
      <tbody>
        {% for result in results.data %}
          <tr>
            <td class="border px-4 py-2">{{ result.label }}</td>
            {% for item in result.data %}
              <td class="border px-4 py-2">{{ item }}</td>
            {% endfor %}
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div> #}

  {# <div class="max-w-2xl mx-auto mb-12">
    <h3 class="text-lg font-bold mb-2 text-center">Build Scaling Performance - Large Sites</h2>
    <canvas id="results-large-chart" width="400" height="400"></canvas>
  </div> #}

  <div class="max-w-2xl mx-auto mb-12">
    <h3 class="text-lg font-bold mb-2 text-center">Build Scaling Performance - Small Sites</h2>
    <canvas id="results-small-chart" width="400" height="400"></canvas>
  </div>

  <div class="max-w-2xl mx-auto mb-12">
    <h3 class="text-lg font-bold mb-2 text-center">Base Build Times</h2>
    <canvas id="results-base-chart" width="400" height="400"></canvas>
  </div>
</div>
